{
  "name": "autodoc",
  "version": "0.1.0",
  "description": "Pipeline-native AI documentation — PR summaries, ADRs, and onboarding guides from code changes",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "openai": "^4.47.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.4.0"
  }
}
